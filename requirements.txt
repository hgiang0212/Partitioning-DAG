pika~=1.3.2
torch
torchvision
numpy
requests~=2.32.3
pyyaml
opencv-python
ultralytics
psutil
torchmetrics
paramiko          # guide/11: one long-lived ssh session to the queue host.
                  # Optional — without it broker_ram falls back to the management
                  # API and labels every line source=rabbitmq_api.